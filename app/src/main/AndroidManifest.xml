<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hailameci">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/hailameci_200x200"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hailameci_200x200"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SignUpActivity"></activity>
        <activity android:name=".activities.LogInActivity" />
        <activity
            android:name=".activities.AddEditPlanActivity"
            android:parentActivityName=".activities.HomeActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>